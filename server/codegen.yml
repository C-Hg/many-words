overwrite: true
generates:
  src/graphql/authorization.types.tsx:
    schema: "http://localhost:4000/authorization"
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-resolvers"
      - "typescript-react-apollo"
    config:
      enumsAsTypes: true
      skipTypename: true
# TODO: add expired token for dev mode
  src/graphql/learn.types.tsx:
    schema: "http://localhost:4000/learn"
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-resolvers"
      - "typescript-react-apollo"
    config:
      enumsAsTypes: true
      skipTypename: true
    
# TODO: separate file for server and web-app